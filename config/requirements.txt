# Production RNN-T Server Requirements
# Optimized for GPU deployment with CUDA support

# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
speechbrain>=1.0.0

# Web server
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6

# Audio processing
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.21.0

# AWS integration
boto3>=1.26.0
botocore>=1.29.0

# Utilities
pydantic>=2.0.0
python-json-logger>=2.0.0
psutil>=5.9.0

# Optional: For enhanced performance
# nvidia-ml-py3>=7.352.0  # GPU monitoring
# tensorrt>=8.6.0         # TensorRT optimization (if available)

# Development/testing (comment out for production)
# pytest>=7.0.0
# httpx>=0.24.0  # For testing FastAPI endpoints